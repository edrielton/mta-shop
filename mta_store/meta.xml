<?xml version="1.0" encoding="UTF-8"?>
<meta>
    <info
        author="MTA Store"
        version="1.1.0"
        description="MTA Store - Ativação automática de itens comprados no site"
        type="misc"
    />

    <!-- Configuração (carregada primeiro) -->
    <script src="config.lua"  type="server" cache="false"/>

    <!-- Lógica principal do servidor -->
    <script src="server.lua"  type="server" cache="false"/>

    <!-- Endpoint de ativação: POST /mta_store/activate -->
    <!-- Endpoint de health:   GET  /mta_store/health   (NOVO) -->
    <html src="activate.lua" default="false" raw="false"/>

    <!-- Arquivo de dados da fila offline (gerado automaticamente) -->
    <file src="queue.json"/>
</meta>
